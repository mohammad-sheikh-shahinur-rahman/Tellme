# Database Choice (firebase or mysql)
DB_TYPE=firebase

# MySQL Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tellme_db
DB_PORT=3306

# Firebase Server Config (Optional if using Client SDK, but recommended for Admin tasks)
# FIREBASE_SERVICE_ACCOUNT_JSON=...
